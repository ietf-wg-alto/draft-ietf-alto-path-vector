# Changes Since Version -04 #

- Define "ane" as the only cost metrics of the cost mode "path-vector".
- Add a new attribute in IRDResourceEntry for the reference of the property map.
- Put forward the concept of query-specific to correlate the response of (Filtered) Cost Map/Endpoint Cost Service and the query of Network Element Property Map.
